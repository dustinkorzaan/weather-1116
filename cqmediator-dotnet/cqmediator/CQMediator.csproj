<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Homegrown mediator-pattern implementation. Must never depend on a
         third-party mediator-pattern NuGet package — do not add a
         PackageReference to one. -->
    <RootNamespace>CQMediator</RootNamespace>
    <AssemblyName>CQMediator</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.11" />
  </ItemGroup>

</Project>
