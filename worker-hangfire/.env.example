# worker-hangfire local settings (copy to .env; do not commit real values)

# Durable Hangfire storage connection string (SQL Server / PostgreSQL).
# Left blank for now — the worker uses in-memory storage until a DB is wired up.
# DB_CONNECTION_STRING=
