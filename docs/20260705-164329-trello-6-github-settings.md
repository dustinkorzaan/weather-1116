# 6-github-settings Work Log

Story: Add a GitHub setting or `/docs/` markdown file to describe this site
as 4 unique projects with feature parity (MVC, API, React UI, Blazor UI).

- Reviewed the repository layout: `mvc-dotnet/WeatherMVC`, `api-dotnet/WeatherAPI`,
  `ui-react`, and `ui-blazor/WeatherBlazor`,Documentation added (no staging/commit done, per instructions):

- **`README.md`** — updated with a table of the 4 projects (MVC, API, React UI, Blazor UI) and a link to the new architecture doc.
- **`docs/architecture.md`** — new file describing the site as four parallel implementations of a weather-forecast sample, their feature-parity goal, relationships (Blazor→API), and CI build process.
- **`docs/20260705-164329-trello-6-github-settings.md`** — filled the auto-created placeholder with a work log following the repo's existing convention.

