# PR Review Log 150

PR: https://github.com/dustinkorzaan/weather-1116/pull/150

## Summary
- Scope: Rename both MCP hosts so the hosting model is explicit — `mcp-dotnet` → `mcp-srv-app-service`, `mcp-function` → `mcp-srv-func-app` — across folders, projects, namespaces, About node names, env vars, `serverLabel`s, workflows, and docs.
- Risk: Medium. No behavior change in code paths, but the env var rename is a hard cutover: `MCP_APP_*` / `MCP_FUNCTION_*` are read with `?? throw`, so API/MVC/worker AI-weather and Chat1b/Chat2b break at runtime until the GitHub `vars`/`secrets`, Azure app settings, and local `.env` files are renamed. Deploy workflows will happily set empty values if the `PROD_MCP_SRV_*` vars/secrets don't exist yet.
- Positives:
  - Rename is genuinely complete and self-consistent: `rg` over the head branch finds no remaining `MCP_APP`, `MCP_FUNCTION`, `mcp-dotnet`, `mcp-function`, `MyMCP*`, `mcpapp`, or `mcpfunc` references outside a few cosmetic ones (below).
  - Tests, About-tree builders, `.env.example`s, devcontainer port labels, `.vscode` tasks/launch, `Weather.sln`, and `docs/` were all updated together — no orphaned project GUIDs, and CI is green (26/26).
  - Breaking changes and the required manual Azure/GitHub follow-ups are documented in the PR body with an old→new mapping.
- Concerns (see inline comments):
  1. `[major]` `.vscode/settings.json` `azureFunctions.preDeployTask` still references the old task label `"publish mcp function"` — the only actually broken reference left after the rename.
  2. `[major]` `FoundryConsoleV4/Program.cs` hardcodes the new app names but keeps the *old* Azure-generated hostname suffixes; recreated App Service / Function App resources get fresh suffixes, so those URLs will not resolve. Suggest sourcing them from `MCP_SRV_*_URL` env vars instead.
  3. `[nit]` `mcpDotNetTask` / `mcpFunctionTask` locals in the API and MVC `AboutController`s keep the retired vocabulary.
  4. `[nit]` Leftover cosmetic old names: test methods `Get_ReturnsHealthyMcpDotNetNode_WhenKeyAndToolConfigured` and `About_ReturnsHealthyMcpFunctionNode`, and the `McpDotNet` mermaid node id in `docs/architecture.md`.
- Deployment ordering note (not a code issue): the two renamed deploy workflows target `weather1116-prod-mcp-srv-app-service` / `-func-app` and resolve the resource group by app name, failing fast if it doesn't exist. So merging before the Azure resources exist will red the prod deploy pipelines rather than mis-deploy — safe, but worth sequencing the resource creation and the `PROD_MCP_SRV_*` vars/secrets before merge.

## Decision
Request changes — two concrete fixes (items 1 and 2); everything else is optional polish.

## Next steps
1. Fix the stale `azureFunctions.preDeployTask` label in `.vscode/settings.json`.
2. Replace the hardcoded FoundryConsoleV4 MCP server URIs with the `MCP_SRV_FUNC_APP_URL` / `MCP_SRV_APP_SERVICE_URL` env vars (or update them with the real new hostnames once the resources exist).
3. Optionally sweep the remaining cosmetic old names (locals, test method names, mermaid id).
4. Before merging: create/rename the Azure resources, add `PROD_MCP_SRV_APP_SERVICE_URL/KEY` and `PROD_MCP_SRV_FUNC_APP_URL/KEY` GitHub vars/secrets, update Azure app settings, and refresh local `.env` files from each `.env.example`.
5. Mark the PR ready for review (currently a draft).

---

## Follow-up review

Both `[major]` items verified fixed on `cursor/mcp-srv-rename-13ef`:

- `5debed5` — `azureFunctions.preDeployTask` now `"publish mcp srv func app"`, matching the `tasks.json` label.
- `c266f41` + `5f2c395` — prod app names/hostnames now reflect the resources that actually exist (`weather1116-prod-mcpsrv-appservice-a2ejcbfah9dsf8dj`, `weather1116-prod-mcpsrv-funcapp-e6dhhhbfhuatfvbh`), and they're consistent across both deploy workflows, `FoundryConsoleV4/.env.example`, `FoundryConsoleV4/Program.cs`, and `docs/architecture.md`. `rg` finds no remaining `MCP_APP`, `MCP_FUNCTION`, `MyMCP*`, `mcp-dotnet`, or `mcp-function` references anywhere in the branch.

**[minor]** The PR description's naming matrix and breaking-changes table still list the prod Azure apps as `weather1116-prod-mcp-srv-app-service` / `weather1116-prod-mcp-srv-func-app`, but the real resources are `weather1116-prod-mcpsrv-appservice` / `weather1116-prod-mcpsrv-funcapp`. Worth updating the body so the post-merge checklist matches reality (note the Azure app names intentionally differ from the folder/About-node names).

**[nit]** Optional polish, unchanged from the first pass: `mcpDotNetTask` / `mcpFunctionTask` locals in the API and MVC `AboutController`s, the `Get_ReturnsHealthyMcpDotNetNode_*` / `About_ReturnsHealthyMcpFunctionNode` test method names, and the `McpDotNet` mermaid node id in `docs/architecture.md`.

## Decision
Approve — pending the pre-merge ops steps: `PROD_MCP_SRV_APP_SERVICE_URL/KEY` and `PROD_MCP_SRV_FUNC_APP_URL/KEY` GitHub vars/secrets must exist and the Azure app settings must be renamed, otherwise the deploy workflows will push empty values and the `?? throw` guards will fail at runtime. Also mark the PR ready for review (still a draft).

---

## Follow-up review 2 (`6908c01`)

Verified: `AboutController` task locals (API + MVC), the stale test method names, and the mermaid node ids are all on `mcp-srv-*` naming, and the extra renames of `BuildMcpNodes_DefaultToHealthy` → `BuildMcpSrvNodes_DefaultToHealthy` and `Get_ReturnsUnhealthyNode_WhenMcpApiKeyMissing` → `..._WhenMcpSrvAppServiceKeyMissing` are consistent. Pure renames, no behavior change; `rg` over the branch now finds zero occurrences of `mcp-dotnet`, `mcp-function`, `McpDotNet`, `McpFunction`, `MyMCP*`, `MCP_APP*`, `MCP_FUNCTION*`, `mcpapp`, or `mcpfunc`. Branch is current with `main` (`1cba6d3`), no rebase needed.

Hardcoded prod URLs in `FoundryConsoleV4` are by design for the console learning programs — withdrawing that earlier suggestion; the values themselves are correct.

## Decision
Approve.

## Remaining pre-merge (ops, not code)
1. Update the PR description's app names to the real resources: `weather1116-prod-mcpsrv-appservice` / `weather1116-prod-mcpsrv-funcapp`.
2. Add the `PROD_MCP_SRV_APP_SERVICE_URL/KEY` and `PROD_MCP_SRV_FUNC_APP_URL/KEY` GitHub vars/secrets and rename the Azure app settings — the `?? throw` guards fail closed if these are missing.
3. Mark the PR ready for review (still a draft).
