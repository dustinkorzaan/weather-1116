# 5-react Prompt

Create a new branch and pull request "5-react".

In this poll request, we would like to work on the React site to get it to build and run on port 3000. some of the files are almost ready, but they need to be merged together so that this entire solution works cohesively, when I hit run all in codespaces, not only the api-dotnet and ui-blazor should run, but I also want it to run ui-react give it a try

also store and this prompt in /docs/5-react-prompt.md
also store and push logs of this chat output to /docs/5-react-log-ouyput.md
