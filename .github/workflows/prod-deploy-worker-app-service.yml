name: cicd-weather1116-prod-worker
run-name: CICD weather1116-prod-worker

on:
  workflow_run:
    workflows:
      - build-and-test
    branches:
      - main
    types:
      - completed
  workflow_dispatch:

concurrency:
  group: prod-deploy-worker
  cancel-in-progress: true

env:
  BUILD_NUMBER: ${{ github.run_number }}
  BUILD_BRANCH_NAME: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_branch || github.ref_name }}

jobs:
  build:
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    permissions:
      contents: read #This is required for actions/checkout
    outputs:
      build_start: ${{ steps.build_start.outputs.build_start }}

    steps:
      - name: Record build start time
        id: build_start
        shell: bash
        run: echo "build_start=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.ref }}

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.x'

      - name: Build with dotnet
        run: dotnet build ./worker-dotnet/worker/WeatherWorkerDotNet.csproj --configuration Release

      - name: dotnet publish
        run: dotnet publish ./worker-dotnet/worker/WeatherWorkerDotNet.csproj -c Release -o "${{ github.workspace }}/publish/worker-dotnet"

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v7
        with:
          name: .net-app
          path: ${{ github.workspace }}/publish/worker-dotnet

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment: prod
    env:
      MCP_SRV_APP_SERVICE_URL: ${{ vars.PROD_MCP_SRV_APP_SERVICE_URL }}
      MCP_SRV_FUNC_APP_URL: ${{ vars.PROD_MCP_SRV_FUNC_APP_URL }}
      BUILD_START: ${{ needs.build.outputs.build_start }}
    permissions:
      id-token: write #This is required for requesting the JWT
      contents: read #This is required for actions/checkout

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v7
        with:
          name: .net-app
          path: ${{ github.workspace }}/publish/worker-dotnet

      - name: Debug Azure OIDC secret lengths
        shell: bash
        env:
          AZURE_GITHUB_CLIENTID: ${{ secrets.AZURE_GITHUB_CLIENTID }}
          AZURE_TENANTID: ${{ secrets.AZURE_TENANTID }}
          AZURE_SUBSCRIPTIONID: ${{ secrets.AZURE_SUBSCRIPTIONID }}
        run: |
          echo "AZURE_GITHUB_CLIENTID length=${#AZURE_GITHUB_CLIENTID}"
          echo "AZURE_TENANTID length=${#AZURE_TENANTID}"
          echo "AZURE_SUBSCRIPTIONID length=${#AZURE_SUBSCRIPTIONID}"

      - name: Login to Azure
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_GITHUB_CLIENTID }}
          tenant-id: ${{ secrets.AZURE_TENANTID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTIONID }}

      - name: Set App Service settings
        uses: azure/cli@v3
        with:
          inlineScript: |
            APP_RG=$(az webapp list --query "[?name=='weather1116-prod-worker'].resourceGroup | [0]" -o tsv)
            if [ -z "$APP_RG" ]; then
              echo "Could not resolve resource group for weather1116-prod-worker" >&2
              exit 1
            fi

            az webapp config appsettings set \
              --name weather1116-prod-worker \
              --resource-group "$APP_RG" \
              --settings \
                DB_CONNECTION_STRING="${{ secrets.AZURE_SQL_DB_CONNECTION_STRING }}" \
                AZURE_FOUNDRY_PROD_EUS2_PROJ_URL="${{ vars.AZURE_FOUNDRY_PROD_EUS2_PROJ_URL }}" \
                AZURE_FOUNDRY_PROD_EUS2_KEY="${{ secrets.AZURE_FOUNDRY_PROD_EUS2_KEY }}" \
                AZURE_FOUNDRY_PROD_EUS2_MODEL="${{ vars.AZURE_FOUNDRY_PROD_EUS2_MODEL }}" \
                MCP_SRV_APP_SERVICE_URL="${{ env.MCP_SRV_APP_SERVICE_URL }}" \
                MCP_SRV_FUNC_APP_URL="${{ env.MCP_SRV_FUNC_APP_URL }}" \
                MCP_SRV_FUNC_APP_KEY="${{ secrets.PROD_MCP_SRV_FUNC_APP_KEY }}" \
                MCP_SRV_APP_SERVICE_KEY="${{ secrets.PROD_MCP_SRV_APP_SERVICE_KEY }}" \
                ASPNETCORE_ENVIRONMENT=Production \
                ASPNETCORE_DETAILEDERRORS=false \
                BUILD_NUMBER="${{ env.BUILD_NUMBER }}" \
                BUILD_START="${{ env.BUILD_START }}" \
                BUILD_BRANCH_NAME="${{ env.BUILD_BRANCH_NAME }}"

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'weather1116-prod-worker'
          slot-name: 'Production'
          package: ${{ github.workspace }}/publish/worker-dotnet
