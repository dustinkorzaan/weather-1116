[project]
name = "foundry-console-v2-python"
version = "0.1.0"
description = "Python port of FoundryConsoleV2: model direct against the Foundry unified AI services endpoint (Responses API)."
requires-python = ">=3.11"
dependencies = [
    "openai>=1.80",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
foundry-console-v2-python = "foundry_console_v2:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["foundry_console_v2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
