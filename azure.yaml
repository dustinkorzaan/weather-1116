# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: weather1116

# Infra-only: azd provisions wx1116-prod-* resources (App Service + Function
# App + SQL + SWA + Foundry) via Bicep. Application deploys use GitHub
# Actions workflows (OIDC + dotnet publish + az webapp/functionapp deploy /
# Azure/static-web-apps-deploy).

infra:
  provider: bicep
  path: infra
  module: main
