# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: weather1116

# Infra-only: azd provisions wx1116-prod-* resources (ACA + ACR + SQL + SWA +
# Foundry) via Bicep. Application deploys use GitHub Actions workflows
# (OIDC + az acr build + az containerapp update / Azure/functions-action /
# Azure/static-web-apps-deploy). See docs/aca-bootstrap.md for first-deploy steps.

infra:
  provider: bicep
  path: infra
  module: main
