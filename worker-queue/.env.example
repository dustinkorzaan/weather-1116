# worker-queue local settings (copy to .env; do not commit real values)

# Azure Service Bus connection string (or namespace + key).
# Left blank for now — the worker runs idle until the namespace/key are provisioned.
# Queue__ConnectionString=
# Queue__QueueName=weather-jobs
